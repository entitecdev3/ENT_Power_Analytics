<mvc:View controllerName="entitec.pbi.embedding.controller.App" displayBlock="true"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.f"
    xmlns="sap.m">

    <Page id="idAppPage" showHeader="{appView>/headerVisible}">
        <headerContent>
            <core:Fragment fragmentName="entitec.pbi.embedding.fragments.MainToolbar" type="XML" />
        </headerContent>
        <App id="idApp">
            <!-- <f:FlexibleColumnLayout id="layout" layout="{appView>/layout}" backgroundDesign="Translucent" /> -->
        </App>
    </Page>
</mvc:View>
